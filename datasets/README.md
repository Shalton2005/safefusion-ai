# Datasets

Placeholder storage for project datasets and derived data assets.

Planned areas:

- `raw/` for immutable source datasets
- `processed/` for cleaned and transformed datasets
- `external/` for third-party data drops
- `annotations/` for labels and metadata